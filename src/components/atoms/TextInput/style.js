import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textInput: {
        borderWidth: 1,
        borderColor: '#020202',
        borderRadius: 8,
        paddingHorizontal: 10,
        paddingTop: 7,
        paddingBottom: 10
    }
})